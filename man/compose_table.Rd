% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compose_table.R
\name{compose_table}
\alias{compose_table}
\title{Compose a frequency table with optional grouping}
\usage{
compose_table(
  data,
  var,
  group = NULL,
  weight = NULL,
  prop = "none",
  na.rm = TRUE
)
}
\arguments{
\item{data}{A data frame or data.table containing the variables to tabulate.}

\item{var}{Character string specifying the name of the variable to tabulate.}

\item{group}{Character vector of variable name(s) to group by. If \code{NULL} (default),
creates a simple frequency table. If provided, creates cross-tabulations with
each grouping variable. Group columns are prefixed with \verb{[group_name]} in the
output.}

\item{weight}{Character string specifying the name of the weight variable to use
for weighted frequencies.}

\item{prop}{Character string specifying proportion type. Options are \code{"none"} for
counts only, \code{"col"} for column proportions, or \code{"row"} for row proportions.
Default is \code{"none"}.}

\item{na.rm}{Should missing values be removed from the table? Defaults to TRUE.}
}
\value{
A data.table with the following structure:
\item{label}{Variable label from the \code{var} attribute (if available)}
\item{var}{Factor levels of the tabulated variable}
\item{n}{Weighted counts}
\item{total}{Overall proportions (if \code{prop != "none"})}
\item{\link{group} level}{One column per level of each grouping variable (if \code{group} is specified)}
}
\description{
Creates a frequency table for a categorical variable, optionally cross-tabulated
by one or more grouping variables. The function automatically adds variable labels
and standardizes column names for easier downstream processing.
}
\examples{
compose_table(penguins, var = "species", group = "island", prop = "col")
}
