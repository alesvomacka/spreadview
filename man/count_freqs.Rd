% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_freqs.R
\name{count_freqs}
\alias{count_freqs}
\title{Calculate Frequency Tables with Optional Proportions}
\usage{
count_freqs(data, var, group = NULL, weight = NULL, prop = "none")
}
\arguments{
\item{data}{A data frame or object that can be coerced to a data.table.}

\item{var}{Character string specifying the name of the variable to tabulate.}

\item{group}{Character string specifying the name of the grouping variable,
or \code{NULL} (default) for no grouping.}

\item{weight}{Character string specifying the name of the weight variable
to use for weighted frequencies. If no weights are specified, equal weights
are assumed.}

\item{prop}{Character string indicating the type of proportion to calculate.
Options are:
\itemize{
\item \code{"none"} (default): Return weighted counts only
\item \code{"total"}: Proportions of the grand total
\item \code{"row"}: Proportions within each row (within each level of \code{var})
\item \code{"col"}: Proportions within each column (within each level of \code{group})
}}
}
\value{
A data.table containing:
\itemize{
\item When \code{group} is \code{NULL}: A table with \code{var} and either \code{n} (counts) or
\code{total} (proportions)
\item When \code{group} is specified and \code{prop != "none"}: A wide-format table with
\code{var} as rows and \code{group} levels as columns, containing proportions
\item When \code{group} is specified and \code{prop == "none"}: A long-format table with
\code{var}, \code{group}, and \code{n} columns
}
}
\description{
Computes weighted frequency tables for a variable, optionally grouped by
another variable. Can return counts or proportions calculated across
different margins (total, row, or column).
}
\examples{
count_freqs(data = penguins, var = "species")
count_freqs(data = penguins, var = "species", group = "island", prop = "col")

}
